{{! pagination template.}}
<section id="pagination">
  <h3 class="pagenum"><br>Page {{page}} of {{pages}}</h3>
  <ul>
    {{#if prev}}
      <li class="prev"><a href="{{page_url prev}}">Previous Page</a></li>
    {{/if}}
    {{#if next}}
      <li class="next"><a href="{{page_url next}}">Next Page</a></li>
    {{/if}}
  </ul>
</section>
{{! pagination template.}}
